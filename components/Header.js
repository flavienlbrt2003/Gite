import styles from "../styles/home.module.css";

const Header = () => {
    return (
        <div className={styles.top}>
            <h1 className={styles.h1}>Gîte NC</h1>
        </div>
    );
};

export default Header;